lifeline:
  build:
    base: [ go@1 ]
    build:
      - export GOMODCACHE="/build/source/mod" && go build -o lifeline-server main.go
    deploy: [ lifeline-server ]
    cache: [ mod ]
  run:
    start: ./lifeline-server
